---
import Layout from '../layouts/Layout.astro';
import WeatherStation from '../components/WeatherStation.astro';
import MoodRecorder from '../components/MoodRecorder.tsx';

export const prerender = false; // SSR模式
---

<Layout title="SunSpot 向日葵 - 动态心情气象站">
  <div class="container">
    <section class="hero">
      <div class="hero-content">
        <p class="hero-eyebrow">SunSpot 心情气象站</p>
        <h1 class="hero-title">
          <span class="sunflower animate-float">🌻</span>
          欢迎来到 SunSpot
        </h1>
        <p class="hero-subtitle">
          用阳光记录每一天的心情
        </p>
        <div class="hero-actions">
          <a href="/diary" class="btn">开始记录</a>
          <a href="/diary" class="btn btn-ghost">浏览日记</a>
        </div>
        <p class="hero-note">把情绪天气写成一页暖色手账。</p>
      </div>
    </section>
    
    <section class="dashboard">
      <div class="dashboard-grid">
        <div class="weather-section">
          <h2 class="section-title">实时天气</h2>
          <WeatherStation />
        </div>
        
        <div class="mood-section">
          <h2 class="section-title">我的心情</h2>
          <MoodRecorder client:load />
        </div>
      </div>
    </section>
    
    <section class="quick-actions">
      <a href="/diary" class="action-card glass">
        <span class="action-icon">📔</span>
        <div class="action-content">
          <h3>写日记</h3>
          <p>用Markdown记录今天的故事</p>
        </div>
      </a>
      <a href="/diary" class="action-card glass">
        <span class="action-icon">📅</span>
        <div class="action-content">
          <h3>日历视图</h3>
          <p>查看有阳光标记的日子</p>
        </div>
      </a>
    </section>
  </div>
</Layout>

<style>
  .hero {
    text-align: center;
    padding: var(--spacing-2xl) 0 var(--spacing-xl);
    position: relative;
  }

  .hero::before {
    content: '';
    position: absolute;
    inset: -10% 10% 30% 10%;
    background: radial-gradient(480px circle at center, color-mix(in srgb, var(--primary) 45%, transparent), transparent 70%);
    opacity: 0.8;
    z-index: -1;
  }

  .hero-content {
    max-width: 760px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: var(--spacing-sm);
  }

  .hero-eyebrow {
    font-size: 0.7rem;
    letter-spacing: 0.38em;
    text-transform: uppercase;
    color: var(--text-muted);
  }

  .hero-title {
    font-size: clamp(2.4rem, 4.8vw, 4.2rem);
    font-weight: 700;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--spacing-sm);
    color: var(--ink);
  }

  .sunflower {
    font-size: clamp(3rem, 6vw, 4.5rem);
    filter: drop-shadow(0 12px 24px rgba(60, 40, 20, 0.2));
  }

  .hero-subtitle {
    font-size: 1.2rem;
    color: var(--text-muted);
  }

  .hero-actions {
    display: flex;
    gap: var(--spacing-sm);
    justify-content: center;
    flex-wrap: wrap;
    margin-top: var(--spacing-sm);
  }

  .hero-note {
    font-size: 0.95rem;
    color: var(--text-muted);
  }

  .section-title {
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: 0.22em;
    margin-bottom: var(--spacing-md);
  }

  .dashboard {
    margin: var(--spacing-2xl) 0;
  }

  .dashboard-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: var(--spacing-lg);
  }

  .weather-section,
  .mood-section {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-md);
  }

  .quick-actions {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--spacing-md);
    margin-top: var(--spacing-xl);
  }

  .action-card {
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
    padding: var(--spacing-lg);
    text-decoration: none;
    color: var(--text);
    position: relative;
    overflow: hidden;
  }

  .action-card::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(120deg, color-mix(in srgb, var(--primary) 25%, transparent), transparent 60%);
    opacity: 0;
    transition: opacity var(--transition-normal);
    pointer-events: none;
  }

  .action-card:hover::after {
    opacity: 1;
  }

  .action-icon {
    font-size: 2.8rem;
  }

  .action-content h3 {
    font-size: 1.25rem;
    font-weight: 700;
    margin-bottom: 4px;
    color: var(--ink);
  }

  .action-content p {
    font-size: 0.95rem;
    color: var(--text-muted);
  }

  @media (max-width: 768px) {
    .hero-title {
      flex-direction: column;
    }
  }
</style>
