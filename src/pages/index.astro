---
import Layout from '../layouts/Layout.astro';
import WeatherStation from '../components/WeatherStation.astro';
import MoodRecorder from '../components/MoodRecorder.tsx';

export const prerender = false; // SSR模式
---

<Layout title="SunSpot 向日葵 - 动态心情气象站">
  <div class="container">
    <section class="hero">
      <div class="hero-content">
        <h1 class="hero-title">
          <span class="sunflower animate-float">🌻</span>
          欢迎来到 SunSpot
        </h1>
        <p class="hero-subtitle">
          用阳光记录每一天的心情
        </p>
      </div>
    </section>
    
    <section class="dashboard">
      <div class="dashboard-grid">
        <div class="weather-section">
          <h2 class="section-title">实时天气</h2>
          <WeatherStation />
        </div>
        
        <div class="mood-section">
          <h2 class="section-title">我的心情</h2>
          <MoodRecorder client:load />
        </div>
      </div>
    </section>
    
    <section class="quick-actions">
      <a href="/diary" class="action-card glass">
        <span class="action-icon">📔</span>
        <div class="action-content">
          <h3>写日记</h3>
          <p>用Markdown记录今天的故事</p>
        </div>
      </a>
      <a href="/diary" class="action-card glass">
        <span class="action-icon">📅</span>
        <div class="action-content">
          <h3>日历视图</h3>
          <p>查看有阳光标记的日子</p>
        </div>
      </a>
    </section>
  </div>
</Layout>

<style>
  .hero {
    text-align: center;
    padding: var(--spacing-xl) 0;
  }
  
  .hero-title {
    font-size: 3rem;
    font-weight: 700;
    margin-bottom: var(--spacing-sm);
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--spacing-sm);
  }
  
  .sunflower {
    font-size: 4rem;
  }
  
  .hero-subtitle {
    font-size: 1.25rem;
    color: var(--text-muted);
  }
  
  .section-title {
    font-size: 1rem;
    font-weight: 600;
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: var(--spacing-md);
  }
  
  .dashboard {
    margin: var(--spacing-xl) 0;
  }
  
  .dashboard-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: var(--spacing-lg);
  }
  
  .quick-actions {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--spacing-md);
    margin-top: var(--spacing-xl);
  }
  
  .action-card {
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
    padding: var(--spacing-lg);
    text-decoration: none;
    color: var(--text);
  }
  
  .action-icon {
    font-size: 2.5rem;
  }
  
  .action-content h3 {
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 4px;
  }
  
  .action-content p {
    font-size: 0.9rem;
    color: var(--text-muted);
  }
  
  @media (max-width: 768px) {
    .hero-title {
      font-size: 2rem;
      flex-direction: column;
    }
    
    .sunflower {
      font-size: 3rem;
    }
  }
</style>
